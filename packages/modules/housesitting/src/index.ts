/**
 * The housesitting module (spec §5.10).
 *
 * One composition, three outputs: a print-perfect PDF, the `/sitter` login,
 * and kiosk Housesitter Mode. Composed from live records rather than generated
 * and saved — a guide that was right when it was written is wrong the first
 * time an animal moves pens, which is the week it is most likely to be read.
 */

export * from "./domain/care-guide.js";
