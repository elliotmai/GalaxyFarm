/**
 * The horses module (spec §5.9) — a placeholder with one live part.
 *
 * The module itself is stub routes and a Roadmap that shares the kernel's
 * generic aggregate. What is real now is the candidate extension: §5.9 says
 * horses are "the purchase furthest out and the one most worth researching
 * slowly, so the shopping surface is live long before the module is."
 *
 * When horses arrive, the build is filling in a prepared module rather than
 * designing one.
 */

export * from "./domain/horse-candidate.js";
