node_modules
.next
.turbo
dist
coverage
playwright-report
test-results
pnpm-lock.yaml

# The spec and mockups are authored documents, not generated code. Prettier
# would repaginate every markdown table and bury real edits in whitespace churn.
docs/

# Generated by drizzle-kit. Reformatting them would produce a diff on every
# `db:generate` and hide the real schema change underneath it.
packages/infrastructure/db/migrations/

# Font data, not stylesheet. One `@font-face` per line, each holding a base64
# blob a few hundred kilobytes long — there is nothing in here for Prettier to
# make more readable, and wrapping it would only make the diffs enormous.
tools/webfonts.css
