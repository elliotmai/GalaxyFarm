import { PagePlaceholder } from "../../_components/page-placeholder";

export const metadata = { title: "Care Guide" };

export default function SitterPage() {
  return <PagePlaceholder title={"Care Guide"} route={"/sitter"} phase={"Phase 4"} />;
}
