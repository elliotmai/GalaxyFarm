import { PagePlaceholder } from "../../_components/page-placeholder";

export const metadata = { title: "Kiosk" };

export default function KioskPage() {
  return <PagePlaceholder title={"Kiosk"} route={"/kiosk"} />;
}
