import { PagePlaceholder } from "../../../_components/page-placeholder";

export const metadata = { title: "Shows" };

export default function AccountShowsPage() {
  return <PagePlaceholder title={"Shows"} route={"/account/shows"} phase={"Phase 5"} />;
}
