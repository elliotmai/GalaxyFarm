import { PagePlaceholder } from "../../_components/page-placeholder";

export const metadata = { title: "My Animals" };

export default function AccountPage() {
  return <PagePlaceholder title={"My Animals"} route={"/account"} phase={"Phase 5"} />;
}
