import { PagePlaceholder } from "../../../_components/page-placeholder";

export const metadata = { title: "Forms" };

export default function AccountFormsPage() {
  return <PagePlaceholder title={"Forms"} route={"/account/forms"} phase={"Phase 5"} />;
}
