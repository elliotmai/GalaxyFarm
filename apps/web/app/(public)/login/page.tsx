import { PagePlaceholder } from "../../_components/page-placeholder";

export const metadata = { title: "Sign In" };

export default function LoginPage() {
  return <PagePlaceholder title={"Sign In"} route={"/login"} />;
}
