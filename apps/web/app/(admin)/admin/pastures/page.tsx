import { PagePlaceholder } from "../../../_components/page-placeholder";

export const metadata = { title: "Pasture Care" };

export default function AdminPasturesPage() {
  return <PagePlaceholder title={"Pasture Care"} route={"/admin/pastures"} />;
}
