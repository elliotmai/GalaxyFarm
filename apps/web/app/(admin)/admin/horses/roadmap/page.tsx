import { PagePlaceholder } from "../../../../_components/page-placeholder";

export const metadata = { title: "Horse Roadmap" };

export default function AdminHorsesRoadmapPage() {
  return (
    <PagePlaceholder
      title={"Horse Roadmap"}
      route={"/admin/horses/roadmap"}
      phase={"a later phase"}
    />
  );
}
