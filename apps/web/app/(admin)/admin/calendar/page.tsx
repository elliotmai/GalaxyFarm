import { PagePlaceholder } from "../../../_components/page-placeholder";

export const metadata = { title: "Calendar" };

export default function AdminCalendarPage() {
  return <PagePlaceholder title={"Calendar"} route={"/admin/calendar"} />;
}
