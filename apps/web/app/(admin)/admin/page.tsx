import { PagePlaceholder } from "../../_components/page-placeholder";

export const metadata = { title: "Dashboard" };

export default function AdminPage() {
  return <PagePlaceholder title={"Dashboard"} route={"/admin"} />;
}
