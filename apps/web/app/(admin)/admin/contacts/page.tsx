import { PagePlaceholder } from "../../../_components/page-placeholder";

export const metadata = { title: "Contacts" };

export default function AdminContactsPage() {
  return <PagePlaceholder title={"Contacts"} route={"/admin/contacts"} />;
}
