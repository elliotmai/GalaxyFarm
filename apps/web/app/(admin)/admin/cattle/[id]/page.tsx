import { PagePlaceholder } from "../../../../_components/page-placeholder";

export const metadata = { title: "Animal Profile" };

export default function AdminCattleIdPage() {
  return <PagePlaceholder title={"Animal Profile"} route={"/admin/cattle/[id]"} />;
}
