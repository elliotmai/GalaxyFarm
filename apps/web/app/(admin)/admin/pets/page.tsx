import { PagePlaceholder } from "../../../_components/page-placeholder";

export const metadata = { title: "Pets" };

export default function AdminPetsPage() {
  return <PagePlaceholder title={"Pets"} route={"/admin/pets"} />;
}
