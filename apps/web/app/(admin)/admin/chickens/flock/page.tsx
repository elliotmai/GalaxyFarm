import { PagePlaceholder } from "../../../../_components/page-placeholder";

export const metadata = { title: "Flocks" };

export default function AdminChickensFlockPage() {
  return <PagePlaceholder title={"Flocks"} route={"/admin/chickens/flock"} />;
}
