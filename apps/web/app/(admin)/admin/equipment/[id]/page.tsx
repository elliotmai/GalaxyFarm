import { PagePlaceholder } from "../../../../_components/page-placeholder";

export const metadata = { title: "Equipment Detail" };

export default function AdminEquipmentIdPage() {
  return <PagePlaceholder title={"Equipment Detail"} route={"/admin/equipment/[id]"} />;
}
