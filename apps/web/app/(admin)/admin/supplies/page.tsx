import { PagePlaceholder } from "../../../_components/page-placeholder";

export const metadata = { title: "Supplies" };

export default function AdminSuppliesPage() {
  return <PagePlaceholder title={"Supplies"} route={"/admin/supplies"} />;
}
